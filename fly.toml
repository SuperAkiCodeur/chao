app            = "chao-bot"
primary_region = "cdg"

[build]
  dockerfile = "Dockerfile"

[env]
  TZ         = "Europe/Paris"
  NODE_ENV   = "production"

[processes]
  app = "node_modules/.bin/tsx bot/src/main.ts"

[[vm]]
  memory   = "256mb"
  cpu_kind = "shared"
  cpus     = 1
